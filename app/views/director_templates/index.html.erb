<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1> List of all directors </h1>
  <table border="1">
  <% Director.all.each do |index| %>
    <tr>
      <td><%= index.id %></td> 
      <td>
        <img src="http://robohash.org/<%= index.image %>">
      </td>
      <td><%= index.name %></td> 
      <td><%= index.dob %></td>
      <td><a href="/directors/<%= index.id %>"> Show details </a></td>
    </tr>
  <% end %>  
  </table>
</body>
</html>
